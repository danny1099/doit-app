import { Inter, Open_Sans, Comfortaa } from "next/font/google";

export const globalFont = Inter({ subsets: ["latin"], variable: "--font-global" });
export const titleFont = Open_Sans({ subsets: ["latin"], variable: "--font-title" });
export const brandFont = Comfortaa({ subsets: ["latin"], variable: "--font-brand" });
